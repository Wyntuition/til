### List remote repository(s) 

You might need to check what repository you're working in based on what you cloned, so this command will show you your remote repository(s):

```bash
git remote -v
```
